<!-- /snippets/payment-gateways.liquid -->

{% assign icons = true %}

{% unless shop.enabled_payment_types == empty %}
  {% assign payment_icons_available = 'amazon_payments,amex,bitcoin,cirrus,dankort,diners_club,discover,dogecoin,dwolla,forbrugsforeningen,interac,jcb,litecoin,maestro,mastercard,paypal,visa' | split: ',' %}
  <ul class="inline">
    {% for gateway in shop.enabled_payment_types %}
      {% if payment_icons_available contains gateway %}
        <li>
          {% if icons %}
            {% include 'icon' with gateway %}
          {% else %}
            {{ gateway | camelcase | replace:'_',' ' }}
          {% endif %}
        </li>
      {% endif %}
    {% endfor %}
  </ul>
{% endunless %}
